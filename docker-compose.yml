version: "3.8"

services:
  redis:
    image: redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  backend:
    build: ./chatbot-backend
    ports:
      - "5000:5000"
    depends_on:
      - redis

volumes:
  redis_data: